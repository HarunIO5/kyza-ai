/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.sanity.io', 'images.unsplash.com', 'plus.unsplash.com', 'source.unsplash.com', 'xlbwajpxfttzluwdymao.supabase.co', 'kyza.ai'],
    }
}

module.exports = nextConfig
